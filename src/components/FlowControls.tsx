import { Controls } from '@xyflow/react';
import React from 'react';

const FlowControls = () => {
  return (
    <Controls>
      {/* Settings button moved to sidebar - no longer needed here */}
    </Controls>
  );
};

export default FlowControls;